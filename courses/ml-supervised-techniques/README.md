# ML Supervised Techniques

Covers supervised machine learning: SVMs, decision trees, random forests, gradient boosting, logistic regression, neural networks, and CNNs.

**Status:** draft — content written but PDFs not yet compiled
**Missing:** compiled PDFs; `old_summary.tex` is an earlier draft kept for reference

## Files

| File | Description |
|------|-------------|
| `summary.tex` | Lecture summary (needs PDF compilation) |
| `exams.tex` | Old exam questions with answer key (needs PDF compilation) |
| `old_summary.tex` | Earlier draft — kept for reference, do not edit |
| `figures/` | SVM diagrams, backprop network, RNN, AdaBoost, bias-variance plots |

## How to help

- [ ] Compile `summary.tex` → commit `summary.pdf`
- [ ] Compile `exams.tex` → commit `exams.pdf`
- [ ] Review the answer key in `exams.tex` for correctness
- [ ] Add more exam questions from recent semesters
